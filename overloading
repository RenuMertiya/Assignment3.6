1)Method Overloading with same return types.
The concept of method overloading depends on 2 things-1)method name and 2)signature
It means using same method name with different signatures.
However, using same return type is not an issue here. 
So it is possible to overload methods with same return type.

For example:

class Adder{  
static int add(int a, int b){return a+b;}  
static double add(double a, double b){return a+b;}  
}  
class TestOverloading2{  
public static void main(String[] args){  
System.out.println(Adder.add(11,11));  
System.out.println(Adder.add(12.3,12.6));  
}}  
